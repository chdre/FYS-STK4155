Run by: python main.py (python 3)

main.py depends on functions.py, NeuralNetwork.py the credit card dataset in a folder ...\data\.

Performs a classification using neural network in NeuralNetwork.py and logistic regression on credit card default data. Compares with scikit-learns MLPClassifier.
Performs regression using the neural network and compares with scikit-learns MLPRegressor.
